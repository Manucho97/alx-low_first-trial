#include <stdio.h>
#include "main.h"
/**
 *
 *
 *
 */


void print_to_98(int n)
{
	printf("enter the number %d\n", a);
	scanf("%d", a);

	for (n = a; n <= 98; n++)
	{
		printf("%d", n);
	}
	return (n);
}
